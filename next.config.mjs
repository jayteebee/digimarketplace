/** @type {import('next').NextConfig} */


const nextConfig = {
    images: {
        domains: [
            "localhost",
            "https://digimarketplace-production.up.railway.app"
        ]
    }
}

module.exports = nextConfig;
